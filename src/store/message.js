const DEFAULT_STATE = "Hello World"

//membuat reducer/pecahan data
// eslint-disable-next-line no-unused-vars
export const messageReducer = (state = DEFAULT_STATE, action) => {
  //state isinya default state
  return state;
};
